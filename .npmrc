# Package manager configuration for mac-app-lifecycle-manager

# Don't create package-lock.json (not publishing to npm)
package-lock=false

# Use exact versions in package.json
save-exact=true

# Disable funding messages
fund=false
